name: no2_forecasting
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pandas>=2.0
  - numpy>=1.26
  - scikit-learn>=1.4
  - requests>=2.31
  - lxml>=4.9
  - pip

  - pip:
      - catboost>=1.2.3
      # Optional: hyperparameter tuning
      # - optuna>=3.5.0
      # Optional: plotting
      # - matplotlib>=3.8.0
      # - seaborn>=0.13.0
