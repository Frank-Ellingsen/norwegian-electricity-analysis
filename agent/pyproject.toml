[project]
name = "norway-electricity-agent"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
  "pandas",
  "numpy",
  "matplotlib",
  "statsmodels",
  "google-generativeai",
]

[tool.setuptools]
packages = ["agent"]
